[package]
name = "dg-gherkin"
version.workspace = true
edition = "2021"
description = "Gherkin parsing, semantic validation, and diagram generation for DecisionGraph"

[dependencies]
gherkin = { version = "0.16", features = ["parser"] }
thiserror = "2"

[dev-dependencies]
insta = "1"
